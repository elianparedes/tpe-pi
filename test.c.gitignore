#include "mediaADT.h"
#include <string.h>
#include <stdio.h>
#include <assert.h>
#define BLOCK 20


TContent creaContent ( unsigned long votos , char * titulo , char * tipoTitulo , unsigned short year ,  )

int main ()
{

    mediaADT newMedia = newMediaADT(2004);
    TContent aux;
    char * generos[8] = {"COMEDIA" , "accion" , "Ciencia Ficcion"  , "ACCCION" , "Terror" , "Documental" , "Musical" , "Corto"} ;
    aux = (TContent){
        .numVotes = 120300,
        .primaryTitle= "Cars" ,
        .startYear = 2010,
    };
    aux.genres = malloc(2*sizeof(char *));
    aux.genres[0] = generos[0] ;
    aux.genres[2] = generos[7];

    puts("OK\n");
    return 0;
}